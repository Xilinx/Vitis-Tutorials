name: AIE_F_26-mtxvec
category: canary
platform: [xilinx_vck190_base]
flow: versal
targets:
  vitis_x86_sim:
    timeout: 60m
    run: make all TARGET=x86sim CXX=${XILINX_VITIS}/gnu/aarch64/lin/aarch64-linux/bin/aarch64-linux-gnu-g++ HOST_ARCH=aarch64
  vitis_aie_sim:
    timeout: 60m
    run: make all TARGET=aiesim CXX=${XILINX_VITIS}/gnu/aarch64/lin/aarch64-linux/bin/aarch64-linux-gnu-g++ HOST_ARCH=aarch64
