name: AIE_F_02-gmio_step2
category: canary
platform: [vck190]
flow: versal
targets:
  vitis_aie_sim:
    timeout: 50m
    run: make aiesim TARGET=aiesim CXX=${XILINX_VITIS}/gnu/aarch64/lin/aarch64-linux/bin/aarch64-linux-gnu-g++ HOST_ARCH=aarch64
      EDGE_COMMON_SW=${EDGE_COMMON_SW} SYSROOT=${SYSROOT} EMU_CMD="./launch_hw_emu.sh -run-app embedded_exec.sh" EMBEDDED_PACKAGE_OUT=./
      EMBEDDED_EXEC_SCRIPT=./embedded_exec.sh
