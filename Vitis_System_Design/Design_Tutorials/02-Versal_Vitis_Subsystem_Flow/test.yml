name: VSD_D_02-VSS
category: canary
platform: [vck190, vek280]
flow: versal
env:
  build: env_setup_petalinux.sh
targets:
  vitis_hw_build:
    timeout: 180m
    memory: 64GB
    run: make all TARGET=hw GCC=${XILINX_VITIS}/gnu/aarch64/lin/aarch64-linux/bin/aarch64-linux-gnu-g++
